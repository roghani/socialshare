<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="55dp"
    android:background="#0091CA"
    android:orientation="vertical" >

    <LinearLayout
        android:id="@+id/viwTitleBar"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="center_vertical"
        android:visibility="visible" >

        <Button
            android:id="@+id/btnTitleLeftOp"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginLeft="5dp"
            android:background="@drawable/selector_title_menu" />

        <TextView
            android:id="@+id/txtTitleText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/app_name"
            android:textColor="#FFFFFF"
            android:textSize="18dp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnTitleRightOp"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginRight="5dp"
            android:background="@drawable/selector_title_reload" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/prgTitleProgress"
        style="@style/ProgressBarMidTheme"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:indeterminateOnly="true"
        android:visibility="gone" />

</RelativeLayout>